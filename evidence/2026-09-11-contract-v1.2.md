# Evidence — the conformance run: toolsmith at contract v1.2

**Date:** 2026-09-11. **toolsmith:** `931207b` (fresh clone), and Break 6
re-run at `7ed61b4`. **Environment:** `nix develop` (`IN_NIX_SHELL=impure`),
go1.24.10, git 2.54.0, linux/x86_64.

## 1. What this record is

This is the conformance record for the `contract-v1-2-reconcile` Matter's
Stage 5 step 2: it evidences the reconciliation pass in
`docs/contract-v1-2-reconcile/decisions.md`, read against CONTRACT.md v1.2
at `44cba9d`. That pass closed the five divergences
`docs/binary/decisions.md` §2 left open at contract v1.1 — C2.3, C2.5, C4.2,
C4.4, C4.5 — and this record is the checkable proof: a clean self-check at
v1.2, a fresh tool built from the current skeleton, and one deliberate break
per changed behavior, each showing the new code, message and exit the
reconciliation pass describes.

It supersedes none of `evidence/2026-09-11-toolsmith-conformance.md`, the
v1.1 conformance record — that record's own claims (the eight overclaiming
`[check]` markers, now narrowed at `c48235e`; the C6.3 git-index nuance; the
`--json` divergence, now resolved) still hold as history. This record is the
v1.2 sequel: it re-runs the same shape of check (clone, `make check`,
self-check, a fresh tool) and then walks every behavior the reconciliation
pass changed.

## 2. Reproduction

```
git clone -q /home/dev/Code/toolsmith <scratch>/toolsmith
git -C <scratch>/toolsmith status --short   # empty

cd <scratch>/toolsmith
nix develop --command make check

CGO_ENABLED=0 go build -o <scratch>/bin/toolsmith ./cmd/toolsmith
<scratch>/bin/toolsmith version

mkdir -p <scratch>/xdg-empty
XDG_CONFIG_HOME=<scratch>/xdg-empty <scratch>/bin/toolsmith check <scratch>/toolsmith
XDG_CONFIG_HOME=<scratch>/xdg-empty <scratch>/bin/toolsmith --json check <scratch>/toolsmith
```

Every run that sets an environment variable inline (`XDG_CONFIG_HOME`,
`GIT_CONFIG_GLOBAL`, `GIT_AUTHOR_*`/`GIT_COMMITTER_*`, `TOOLSMITH_CLAUDE_SKILLS_DIR`,
a bare `PATH`) was written as a small `.sh` script under `<scratch>/scripts/`
and run with `bash`; the shells below quote each run's essential command.
The clone kept `.git` throughout, for the same reason
`2026-09-11-toolsmith-conformance.md` §2 gives (C6.3 reads git state).

## 3. `make check`

Exit code: `0`.

```
shellcheck .envrc contrib/check-commit-msg contrib/check-gofumpt
golangci-lint run
0 issues.
go test ./...
ok  	github.com/procrastivity/toolsmith/drift	0.068s
ok  	github.com/procrastivity/toolsmith/internal/checks	0.041s
ok  	github.com/procrastivity/toolsmith/internal/cli	0.642s
ok  	github.com/procrastivity/toolsmith/internal/harness	0.013s
ok  	github.com/procrastivity/toolsmith/internal/harness/registry	0.004s
ok  	github.com/procrastivity/toolsmith/internal/manifest	0.006s
ok  	github.com/procrastivity/toolsmith/internal/verbs/check	1.322s
ok  	github.com/procrastivity/toolsmith/internal/verbs/new	0.910s
(all other toolsmith packages: "[no test files]")
CGO_ENABLED=0 go build -ldflags "-X main.version=931207b -X main.commit=931207b -X main.date=2026-09-11T03:58:51Z" -o bin/toolsmith ./cmd/toolsmith
rm -rf tmp/smoke tmp/smoke-xdg-config
mkdir -p tmp/smoke-xdg-config
XDG_CONFIG_HOME=<clone>/tmp/smoke-xdg-config bin/toolsmith new smoke --dir tmp/smoke --no-git
instantiated smoke at tmp/smoke (module github.com/procrastivity/smoke)
cd tmp/smoke && CGO_ENABLED=0 go build ./... && go vet ./... && go test ./...
ok  	github.com/procrastivity/smoke/internal/checks	0.011s
ok  	github.com/procrastivity/smoke/internal/cli	0.651s
ok  	github.com/procrastivity/smoke/internal/harness	0.016s
ok  	github.com/procrastivity/smoke/internal/harness/registry	0.011s
ok  	github.com/procrastivity/smoke/internal/manifest	0.004s
(all other smoke packages: "[no test files]")
```

Three packages have tests here that the v1.1 record never listed:
`internal/checks`, `internal/harness` and `internal/harness/registry`. The
packages existed at `d94f4ee`, and their tests are new. `internal/checks`
backs `doctor`'s `HarnessTargets` walk, `internal/harness` holds
`Status`, and `internal/harness/registry` holds the C4.2 duplicate-name panic
(§6, Break 1 below). The smoke tool `new` instantiates carries the same two packages,
because the guard and the drift-state code both live in the skeleton, not
only in toolsmith's own tree.

## 4. The self-check

Binary built once, no ldflags:

```
<scratch>/bin/toolsmith version
toolsmith version dev (commit unknown, built unknown)
```

Self-check, `XDG_CONFIG_HOME` pointed at an empty directory:

```
$ XDG_CONFIG_HOME=<scratch>/xdg-empty <scratch>/bin/toolsmith check <scratch>/toolsmith
no findings — mechanical clauses hold for <scratch>/toolsmith
exit=0
```

stderr was empty. `--json`:

```
$ XDG_CONFIG_HOME=<scratch>/xdg-empty <scratch>/bin/toolsmith --json check <scratch>/toolsmith
{"findings":[],"audited":["C1.1","C1.2","C1.6","C2.1","C3.1","C3.4","C3.6","C6.2","C6.3","C6.5","C6.6","C6.7","C7.5"]}
exit=0
```

stderr was empty. `findings` is the empty list, and `audited` is the same
13-clause list `docs/contract-v1-2-reconcile/decisions.md` §3 pins against
`AuditedClauses()` — no marker changed, and no clause was added or dropped
by this Matter. Unlike the v1.1 record, `check --json` now emits this
payload instead of repeating the plain-text lines. The v1.1 record found
C2.3 diverging (§4 there). It is one of the five clauses this Matter
closes, and this is that closure, checked directly rather than read from
the decisions file.

## 5. A freshly instantiated tool

From `<scratch>`, with a hermetic git identity
(`GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_NOSYSTEM=1`, all four
`GIT_AUTHOR_*`/`GIT_COMMITTER_*` set to a fixed test identity — never a real
person's) and `XDG_CONFIG_HOME` pointed at the same empty directory:

```
$ <scratch>/bin/toolsmith new demo --dir <scratch>/demo
instantiated demo at <scratch>/demo (module github.com/procrastivity/demo)

Checklist — the judgment steps the rename cannot do:
  ...
exit=0
```

No environment finding this time: the identity is supplied explicitly, so
`new`'s own C2.5 git preflight (§6, Breaks 3 and 4) never fires. One commit
exists:

```
$ git -C <scratch>/demo log --oneline
3d5fd3c chore: instantiate demo from the toolsmith skeleton
```

`toolsmith check <scratch>/demo`:

```
$ <scratch>/bin/toolsmith check <scratch>/demo
no findings — mechanical clauses hold for <scratch>/demo
exit=0
```

Inside `demo`, `CGO_ENABLED=0 go test ./...` — the skeleton now carries the
drift-state and refusal tests, not only the chassis tests the v1.1 record
saw:

```
$ cd demo && CGO_ENABLED=0 go test ./...
ok  	github.com/procrastivity/demo/internal/checks	0.011s
ok  	github.com/procrastivity/demo/internal/cli	0.529s
ok  	github.com/procrastivity/demo/internal/harness	0.011s
ok  	github.com/procrastivity/demo/internal/harness/registry	0.004s
ok  	github.com/procrastivity/demo/internal/manifest	0.003s
(all other demo packages: "[no test files]")
exit=0
```

`toolsmith --json new demo2 --dir <scratch>/demo2 --no-git`:

```
$ <scratch>/bin/toolsmith --json new demo2 --dir <scratch>/demo2 --no-git
{"name":"demo2","dir":"<scratch>/demo2","module":"github.com/procrastivity/demo2","git":false}
exit=0
```

## 6. Deliberate breaks

Each break exercises one behavior the reconciliation pass changed. Every
break that touches the toolsmith tree runs on a `cp -a` copy of the clone,
never the clone itself. Every break that touches a harness install target
runs against a fresh scratch skills directory, never a real host install.

### Break 1 — C4.2: a duplicate harness registration panics

On a `cp -a` copy of the clone, the claude-code row in
`internal/harness/registry/registry.go`'s `All` was duplicated verbatim,
then the binary was rebuilt and run:

```
$ toolsmith version
panic: registry: duplicate harness name "claude-code" in All

goroutine 1 [running]:
github.com/procrastivity/toolsmith/internal/harness/registry.namesOf(...)
	.../internal/harness/registry/registry.go:74
github.com/procrastivity/toolsmith/internal/harness/registry.init()
	.../internal/harness/registry/registry.go:59
exit=2
```

The binary panics at start, naming the duplicate — as designed at step-01
and recorded in `docs/binary/decisions.md`'s C4.2 note.

### Break 2 — C2.5, name codes: `Acme` is `validation.invalid-name`

```
$ toolsmith --json new Acme --dir <scratch>/acme-should-not-exist
{"error":{"code":"validation.invalid-name","message":"name must be lowercase letters and digits, starting with a letter (it becomes Go package names): got \"Acme\""}}
exit=1
```

`acme-should-not-exist` does not exist afterward. Matches the "Resolution,
part 1" table in `docs/binary/decisions.md`'s C2.5 note: the three name
checks are `validation.*`, exit 1.

### Break 3 — C2.5, git preflight, identity: `not-found.git-identity`

`GIT_CONFIG_GLOBAL` pointed at a file holding only:

```
[user]
	useConfigOnly = true
```

with `GIT_CONFIG_NOSYSTEM=1` and every `GIT_AUTHOR_*`/`GIT_COMMITTER_*`/
`EMAIL` unset:

```
$ toolsmith --json new demo3
{"error":{"code":"not-found.git-identity","message":"git has no commit identity for demo3 (fatal: no email was given and auto-detection is disabled); set user.name and user.email, or pass --no-git"}}
exit=1
$ ls demo3
ls: cannot access 'demo3': No such file or directory
```

`demo3` does not exist afterward: `initGitRepo` removes the target it
created once the identity probe fails. Matches "Resolution, part 1" in the
C2.5 note.

### Break 4 — C2.5, git preflight, PATH: `not-found.git`

Same environment, plus `PATH` pointed at a directory holding no `git`
(the `toolsmith` binary itself invoked by its absolute path, since `PATH`
has nothing to resolve it with either):

```
$ PATH=<scratch>/empty-path-dir <scratch>/bin/toolsmith --json new demo3
{"error":{"code":"not-found.git","message":"git is not on PATH; install it, or pass --no-git"}}
exit=1
$ /bin/ls <scratch>/demo3
ls: cannot access '<scratch>/demo3': No such file or directory
```

No `demo3`. `initGitRepo` checks `git` on `PATH` before creating the
target at all, so nothing was written to remove.

### Break 5 — C2.5/T26 and C2.3, `check`'s verdict

On a `cp -a` copy of the clone, one `actions/checkout` reference in
`.github/workflows/ci.yml` was unpinned from its SHA to `@v4` (one of four
occurrences, so C6.5 has exactly one line to complain about):

```
$ toolsmith check <break-copy>
C6.5: ci.yml: action not SHA-pinned: - uses: actions/checkout@v4 # v4.2.2
```
stderr: `toolsmith: check: 1 finding(s) for <break-copy>`, exit=1.

```
$ toolsmith --json check <break-copy>
{"findings":[{"code":"C6.5","message":"ci.yml: action not SHA-pinned: - uses: actions/checkout@v4 # v4.2.2"}],"audited":["C1.1","C1.2","C1.6","C2.1","C3.1","C3.4","C3.6","C6.2","C6.3","C6.5","C6.6","C6.7","C7.5"]}
```
stderr: `{"error":{"code":"check.findings-present","message":"1 finding(s) for <break-copy>"}}`, exit=1.

Both modes carry the finding on stdout and the `check.findings-present`
verdict on stderr — T26's sanctioned form, resolved for `check` at step-07
(§4 of `docs/contract-v1-2-reconcile/decisions.md`). The v1.1 record's
equivalent break used `exitcode.Silent(1)` and an empty stderr; this is the
behavior that replaced it.

### Break 6 — C4.4, the description asset

`TOOLSMITH_CLAUDE_SKILLS_DIR` at a scratch dir, `XDG_CONFIG_HOME` at a
scratch dir holding
`toolsmith/templates/skills/claude-code/description.txt` with a one-line
override:

```
$ toolsmith install claude-code
installed claude-code skill at <scratch>/desc-skills/toolsmith
exit=0
$ grep '^description:' <scratch>/desc-skills/toolsmith/SKILL.md
description: Use this when the demo scenario needs a one-line override trigger sentence.
```

The override line lands verbatim in `SKILL.md`'s `description:` key.
Rewriting the same asset to two lines and reinstalling:

```
$ toolsmith install claude-code
toolsmith: claudecode: skill description asset "templates/skills/claude-code/description.txt" must be a single line
exit=2
```

**This break found a defect.** The failure was a plain Go error
(`fmt.Errorf` inside `resolveSkillDescription`), not a `toolsmitherr.Error`.
`cli.Execute` treats any such error as a Cobra usage error, so the run
exited 2. C2.4 reserves 2 for usage, and C2.5 wants a structured code.

Step-21 (`7ed61b4`) made the malformed override return
`validation.skill-description`. The same override at `7ed61b4`, with an
empty skills directory:

```
$ toolsmith install claude-code
toolsmith: install: skill description asset "templates/skills/claude-code/description.txt" must be exactly one non-empty line; fix or remove the override under the toolsmith config directory
exit=1
$ toolsmith --json install claude-code
{"error":{"code":"validation.skill-description","message":"skill description asset \"templates/skills/claude-code/description.txt\" must be exactly one non-empty line; fix or remove the override under the toolsmith config directory"}}
exit=1
$ ls <scratch>/skills
(empty)
```

The fallback in `cli.Execute`, which gives exit 2 to every plain verb error,
is older chassis code. `docs/contract-v1-2-reconcile/decisions.md` §2.3
records it, with backlog entry `01M27ACVAM9D3SZSAXZDMZTKQX`.

### Break 7 — C4.5/C4.6/C4.7, the six drift states

`TOOLSMITH_CLAUDE_SKILLS_DIR` at a fresh scratch dir per state, empty
`XDG_CONFIG_HOME`. `toolsmith --json doctor` for each:

| State | `targets[0].state` | Finding code(s) | Exit |
|---|---|---|---|
| missing (nothing installed) | `missing` | (none) | 0 |
| current (after `install claude-code`) | `current` | (none) | 0 |
| modified (hand-edited `SKILL.md`) | `modified` | `advisory.modified-harness-target` | 0 |
| unowned_conflict (stamp deleted, files kept) | `unowned_conflict` | `advisory.unowned-harness-target` | 0 |
| incompatible (stamp JSON unparseable) | `incompatible` | `refusal.incompatible-harness-target` | 1 |
| incompatible (`schemaVersion: 999999`) | `incompatible` | `refusal.incompatible-harness-target` | 1 |
| stale (`makeStale`-style: disk and stamp entry both rewritten to the same wrong value for one file) | `stale` | `advisory.stale-harness-artifact` | 0 |

Raw JSON, one state at a time:

```
missing:
{"findings":[],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/missing-skills/toolsmith","state":"missing"}]}

current:
{"findings":[],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/current-skills/toolsmith","state":"current"}]}

modified:
{"findings":[{"code":"advisory.modified-harness-target","message":"found: <scratch>/drift/modified-skills/toolsmith no longer matches what `toolsmith install claude-code` last wrote — re-run with `toolsmith install claude-code --force`, which destroys the edits"}],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/modified-skills/toolsmith","state":"modified"}]}

unowned_conflict:
{"findings":[{"code":"advisory.unowned-harness-target","message":"found: <scratch>/drift/unowned-skills/toolsmith holds content with no install stamp; it was not written by `toolsmith install claude-code` — re-run with `toolsmith install claude-code --force`, which overwrites files the tool never wrote"}],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/unowned-skills/toolsmith","state":"unowned_conflict"}]}

incompatible (unparseable stamp):
{"findings":[{"code":"refusal.incompatible-harness-target","message":"found: <scratch>/drift/incompatible-unparseable-skills/toolsmith carries a stamp `toolsmith install claude-code` cannot use (unparseable, or from a schema version this binary does not recognize) — re-run with `toolsmith install claude-code --force`, which replaces the stamp"}],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/incompatible-unparseable-skills/toolsmith","state":"incompatible"}]}
stderr: {"error":{"code":"doctor.findings-present","message":"1 finding(s) reported; see above"}}

incompatible (schemaVersion 999999): same shape, dir <scratch>/drift/incompatible-schema-skills/toolsmith.
stderr: {"error":{"code":"doctor.findings-present","message":"1 finding(s) reported; see above"}}

stale:
{"findings":[{"code":"advisory.stale-harness-artifact","message":"found: claude-code harness artifact SKILL.md changed since last install; run `toolsmith install claude-code` to refresh it"}],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/stale-skills/toolsmith","state":"stale"}]}
```

For both `incompatible` fixtures, exit is 1 and stderr carries the
`doctor.findings-present` envelope. The finding keeps the `refusal.`
prefix, because only `incompatible` fails the run (the owner's ruling, under
C4.7). Every other state's stderr is empty.

`install`/`uninstall` on the refusing states, `--json`:

```
install on modified:
{"error":{"code":"refusal.modified-harness-target","message":"refused — <dir> no longer matches what `toolsmith install claude-code` last wrote — re-run with `toolsmith install claude-code --force`, which destroys the edits"}}
exit=3

uninstall on modified:
{"error":{"code":"refusal.modified-harness-target","message":"refused — <dir> no longer matches what `toolsmith install claude-code` last wrote — remove it by hand if that was intentional"}}
exit=3

install on unowned_conflict:
{"error":{"code":"refusal.unowned-harness-target", ...}}
exit=3

install on incompatible (unparseable):
{"error":{"code":"refusal.incompatible-harness-target", ...}}
exit=3
```

`install`'s message names `--force` in every case; `uninstall`'s message
never does — it names removing the tree by hand instead, exactly as
`docs/contract-v1-2-reconcile/decisions.md` §1.4's table states.

`install --force` on the schemaVersion-incompatible tree recovers to
`current`:

```
$ toolsmith install claude-code --force
installed claude-code skill at <scratch>/drift/incompatible-schema-skills/toolsmith
exit=0
$ toolsmith --json doctor
{"findings":[],"targets":[{"harness":"claude-code","dir":"<scratch>/drift/incompatible-schema-skills/toolsmith","state":"current"}]}
exit=0
```

Every state, every code, and every exit matches
`docs/contract-v1-2-reconcile/decisions.md` §1.4 and §1.5's tables exactly.
No break in this section produced a surprise.

## Where this stands

`make check` passes at `931207b`. `toolsmith check` finds nothing on
toolsmith's own tree, nor on a freshly instantiated tool, under both text
and `--json` output — `check --json` now emits `{"findings":[],"audited":
[...]}`, closing the C2.3 divergence the v1.1 record found. The 13-clause
`audited` list is unchanged from v1.1: this Matter touched no `[check]`
marker and no audit code (`docs/contract-v1-2-reconcile/decisions.md` §3).

Every break behaved exactly as
`docs/contract-v1-2-reconcile/decisions.md` describes. `registry.All`
panics on a duplicate name (C4.2). `new`'s three name checks are
`validation.*`, and its git preflight — checked before any file is
written — refuses `not-found.git` or `not-found.git-identity` and leaves no
half-built target (C2.5). `check`'s findings-present verdict is
`check.findings-present` on stderr in both output modes, with the findings
themselves on stdout either way (C2.3, T26). The claude-code skill
description resolves through the asset chain, one line only (C4.4). A
malformed override first exited 2 through a plain error, and step-21 fixed
it to `validation.skill-description` with exit 1 (Break 6). All six drift
states are distinguishable by `doctor --json`'s `targets[0].state`.
`install` and `uninstall` refuse with the right code and name the right
remedy, and only `incompatible` fails `doctor`'s run. `install --force`
recovers the schema-incompatible tree to `current` here, and
`TestInstall_RefusalCodes` covers the other refusing states (C4.5, C4.6,
C4.7).

`docs/contract-v1-2-reconcile/decisions.md` §2.3 records three divergences
in chassis code older than this Matter, each with a backlog entry:

- C2.2 in bare `install`: stdout is not empty on a
  `refusal.harness-targets-refused` failure.
- C4.3 in `claudecode.Uninstall`: refusal policy is still decided inside the
  per-harness package.
- C2.4 in `cli.Execute`: every plain verb error exits 2, as if it were a
  usage error. Break 6 found this one.

This record does not re-verify them. Everything else the reconciliation
pass closed is checked directly above, not only read from the decisions
file.
