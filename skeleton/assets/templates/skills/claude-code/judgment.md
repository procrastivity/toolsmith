TODO(toolname): the one hand-authored judgment paragraph for the
claude-code projection. Say when an agent should reach for toolname at
all, in this harness specifically — the prose that genuinely differs per
harness. Everything else in the generated skill comes from the manifest.
