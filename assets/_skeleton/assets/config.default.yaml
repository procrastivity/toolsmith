# Shipped default tool config. The user override lives at
# $XDG_CONFIG_HOME/toolname/config.yaml and merges over this key-by-key.
# No keys yet — verbs add their own as they earn tool-config scope.
{}
